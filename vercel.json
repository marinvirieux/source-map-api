{
  "version": 2,
  "env": [
    "AIRTABLE_TOKEN",
    "AIRTABLE_BASE_ID"
  ],
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
