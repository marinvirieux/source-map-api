{
  "name": "source-map-api",
  "version": "1.0.0",
  "description": "Airtable API proxy",
  "main": "api/locations.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Serverless functions ready'"
  },
  "engines": {
    "node": "24.x"
  }
}
